import DoctorPortalLayout from '@/components/layouts/DoctorPortalLayout';

export default function DoctorLayout({ children }: { children: React.ReactNode }) {
  return <DoctorPortalLayout>{children}</DoctorPortalLayout>;
}
